# V2 Requirements Acceptance

验收日期：2026-07-21
版本：2.2.3

| 已确认要求 | 实现与验收证据 | 结果 |
| --- | --- | --- |
| 导入大纲或从创意开始两种流程 | 首页双入口；TXT/MD/DOCX 解析预览；创意流五阶段规划 | 通过 |
| 大纲到正文到人工审阅到导出 | 卷章场景树、正文候选、审核写入、TXT/MD/PDF 导出 | 通过 |
| 多 Agent 自动规划 | 定位主题、世界规则、文风边界、人物关系、剧情/时间线/伏笔、卷章场景分别生成 | 通过 |
| 每个规划项独立编辑/重生成/比较/批准 | 每个 Agent 单独 Artifact 与 `series_key`；单项重生成、版本链、双栏比较和批注 | 通过 |
| 所有规划批准前禁止正文 | 后端 `_require_generation_prerequisites` 与全链门禁测试 | 通过 |
| 章级/场景级审核自由切换 | 项目设置；场景生成独立候选并逐场写回章节 | 通过 |
| 自动隐藏 Agent 团队与模型理由 | 阶段内自动执行；任务面板显示模型与选择理由 | 通过 |
| DeepSeek 等官方/兼容 API | DeepSeek、OpenAI、Anthropic、Gemini、xAI、OpenRouter、OpenAI-compatible、自定义 HTTP | 通过 |
| 密钥安全 | Windows Credential Manager 或环境变量；数据库、日志、导出不存明文 | 通过 |
| 质量/费用/速度/均衡路由 | 项目工具栏切换，调用记录保存理由、Token 与费用 | 通过 |
| 参考文风文本提取 | TXT/MD/DOCX 上传，模型提取抽象规则，结果必须审核 | 通过 |
| 直接编辑、AI 修改、重写、方案、版本、批注 | 正文工作台与 Artifact 审核编辑器；右侧“通过并写入正文”会更新章节并即时刷新编辑器 | 通过 |
| 已批准内容仍可修改 | 修改创建新待审核版本，旧版保留为 superseded | 通过 |
| 记忆自动/确认 | 项目级设置，批准后更新章节摘要与资料 | 通过 |
| 大小冲突处理 | 轻微冲突自动校正并标记；重大冲突必须三选一 | 通过 |
| 手动/自动/倒计时续写 | 工具栏切换；批准后自动或倒计时启动下一章；可暂停/立即开始 | 通过 |
| 右栏优先级 | AI 对话、待审核、进度、资料库、费用固定顺序 | 通过 |
| AI 对话自动上下文且修改需审核 | 项目/阶段/章节/选区自动注入；修改生成待确认提案 | 通过 |
| TXT/MD/PDF | 快照与导出窗口提供三种格式 | 通过 |
| 自动/手动保存和三版快照 | 1.6 秒自动保存、保存按钮、普通快照只保留三份 | 通过 |
| AI 修改前与剧情转折永久快照 | 写回前快照；转折检测与手动特殊快照永久保留 | 通过 |
| 关闭窗口后台继续或退出并记忆 | 桌面关闭对话、托盘、记忆配置；异常退出任务下次启动标记中断 | 通过 |
| 项目首页字段 | 书名、阶段、完成字数、待审核数量、最后编辑时间 | 通过 |
| 70% 提醒、110% 当前任务后暂停 | 后端任务结束后预算状态更新，继续前需用户确认 | 通过 |
| 同模型重试 5 次，切换需确认 | 模型请求固定 `max_retries=5`；工作室不自动切换模型 | 通过 |
| 仅深色 Codex 风格 | 单一深色主题，密集工作台布局，桌面/1024 截图验收 | 通过 |
| 真正 Windows EXE、安装包与便携 ZIP | PyInstaller + C# 安装器；控制台和 GUI 生命周期自检 | 通过 |
| 舍弃旧数据 | v2 独立 `NovelAgentStudioV2/data/studio-v2.db` | 通过 |

## V2.2 半成品续写验收

| 已确认要求 | 实现与验收证据 | 结果 |
| --- | --- | --- |
| 文件、粘贴、已有项目三类来源 | 首页导入向导；TXT/MD/DOCX/PDF 文件接口；项目正文克隆 | 通过 |
| 自动提取全部小说资料 | 七个独立分析 Agent 覆盖结构、世界观、人物、时间线、伏笔、文风和未完线 | 通过 |
| 所有分析与补建内容必须审核 | `continuation_analysis`、`continuation_outline`、`continuation_plan` 后端阶段门禁 | 通过 |
| 反向补建卷章场景大纲 | 三个补建 Agent；审核完成前禁止进入续写规划 | 通过 |
| 作者与 AI 方向自由切换 | 续写设置面板保存方向模式和作者大纲；AI 规划结果独立审核 | 通过 |
| 总字数、章节、卷数手动或 AI 建议 | 导入向导和规划面板支持手动值；留空时由规划 Agent 提议并由作者确认 | 通过 |
| 当前章或下一章由作者选择 | 正文阶段起点选择；后端在未选择时拒绝生成 | 通过 |
| 原文永久只读且工作副本可编辑 | `continuation_original` 只读工件、永久导入快照、章节工作副本与版本 | 通过 |
| AI 不得未经批准覆盖已有正文 | 修改模式仍走 `revision_proposal`；续写当前章只追加；批准前不写入 | 通过 |
| 冲突后暂停等待作者 | 重大冲突设置项目暂停状态；作者处理最后一个冲突后解除 | 通过 |
| 沿用单章/连续生成、快照与导出 | 复用手动/自动/倒计时、三版普通快照、永久特殊快照及 TXT/MD/PDF 导出 | 通过 |

## V2.2.1 上下文与右栏滚动验收

| 已确认要求 | 实现与验收证据 | 结果 |
| --- | --- | --- |
| 上下文超过模型限制时自动处理 | 按真实模型窗口预留输出；Prompt 调用前估算 Token 并压缩；Provider 仍报超限时逐级缩小后重试 | 通过 |
| 长篇小说不一次提交整本原文 | 章节分层索引；半成品分析和大纲补建最多 32 片共享 Map-Reduce 汇总；文风参考分片分析后合并 | 通过 |
| 保留关键任务要求 | 压缩保留 Prompt 开头和作者结尾要求，省略中部低优先级上下文并明确插入压缩标记 | 通过 |
| 上下文来源可核对 | ContextBuilder 包含/排除/截断数量、Token 预算和原文分片数写入成果元数据与任务理由 | 通过 |
| 右侧长回复独立滚动 | 右栏限制在工作区高度；聊天标题、消息流、输入框三行布局；只修改消息容器滚动位置 | 通过 |
| 1024px 窗口不遮挡主界面 | Playwright 注入 18 轮对话，验证右栏和输入框底边不超过视口、消息区产生内部滚动、页面无纵向溢出 | 通过 |

## V2.2.2 按钮与项目删除验收

| 已确认要求 | 实现与验收证据 | 结果 |
| --- | --- | --- |
| 删除项目真实生效 | 修复重复开启 SQLAlchemy 事务；组件、后端路由和 Playwright 刷新后持久化三层回归 | 通过 |
| 删除失败不能静默 | 首页显示后端错误并保留原项目；失败组件测试覆盖 | 通过 |
| 每个当前可达按钮都有函数 | TypeScript AST 逐一审计 AppShell、Home、Studio、Models、Custom API 共 104 个按钮，空处理器或缺失处理器直接失败 | 通过 |
| 关键按钮产生真实行为 | 主页、侧栏、模型服务、自定义 Adapter、Manifest、凭据、创作与审核均有组件或浏览器交互测试 | 通过 |
| 最终安装与便携产物可运行 | 安装版和最终 ZIP 解压版分别通过数据库迁移、控制台与 10 秒 GUI 生命周期自检 | 通过 |

## V2.2.3 章节树与总编工作流验收

| 已确认要求 | 实现与验收证据 | 结果 |
| --- | --- | --- |
| 缺失章节进入正确分卷 | 生成计划按相邻分卷起始编号插入缺号，整树按卷位置和章位置返回 | 通过 |
| 重复分卷和乱序可安全修复 | 作者确认后合并同编号卷、迁移章节并按编号重排；缺号才新建空章 | 通过 |
| 原正文与历史不丢失 | 真实 80 章数据库副本验证章节 ID 集合完全一致，场景与版本依附原 ID 保留，修复前永久特殊快照存在 | 通过 |
| 总编可以真正推进工作流 | 明确推进指令产生待确认操作卡；点击执行后调用真实生成服务并创建完成任务与待审核成果 | 通过 |
| 总编不得假装已执行 | 系统提示禁止确认前声称推进；后端未确认时不创建生成成果 | 通过 |

自动化证据：后端 183 tests、前端 42 tests、Ruff、strict mypy、TypeScript、Vite build、5 个 Playwright E2E 全通过。按钮契约、项目删除持久化、WebView2 消息滚动副作用、正文审核写回、80 章分批规划与真实副本修复、总编工作流确认、半成品导入续写、上下文压缩重试、长回复右栏滚动和全文审阅编辑回归测试通过。最终打包目录与 ZIP 解压版均完成控制台和 GUI 自检；产物哈希记录在 `outputs/SHA256SUMS.txt`。
